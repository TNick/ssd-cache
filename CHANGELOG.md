# Changelog

## [Unreleased]

### Changed

- Route CacheMon minifilter MSBuild outputs and intermediates to
  `build/driver/` so the `src/driver/` tree stays source-only.
- Document `service_main.cpp` per project code documentation conventions.
- Document `cachemon_messages.h` per project code documentation conventions.
